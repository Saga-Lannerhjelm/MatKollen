@model List<GroceryListViewModel>

<h2>@ViewData["Title"]</h2>

<ul>
    @{
        foreach(var item in Model)
        {
            <li>@item.ConvertedQuantity@item.Unit @item.FoodItemName</li>
        }
    }
</ul>

